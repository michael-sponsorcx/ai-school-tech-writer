models:
  - name: expiring_agreements
    columns:
      - name: agreement_id
        tests:
          - relationship: 
              to: {{ ref('agreements') }}
              field: agreement_id
      - name: expiration_date
      - name: customer_id
        tests:
          - relationship: 
              to: {{ ref('customers') }}
              field: customer_id